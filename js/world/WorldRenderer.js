window.LumiGame=window.LumiGame||{}; class WorldRenderer{constructor(canvas){this.canvas=canvas;} render(level,player,camera,fx,transform,particles){const c=this.canvas.getContext('2d');c.fillStyle='#10152d';c.fillRect(0,0,c.canvas.width,c.canvas.height);c.fillStyle='#24345a';c.fillRect(0,500,c.canvas.width,80);c.save();c.translate(-camera.x,-camera.y);for(const p of level.platforms){c.fillStyle=p.isOneWay?'#6077a8':'#2f426c';c.fillRect(p.x,p.y,p.width,p.height);}for(const s of level.mainStars.concat(level.secretStars))s.draw(c,camera);for(const cp of level.checkpoints)cp.draw(c,camera);player.draw(c,camera);c.restore();particles.draw(c,camera);}} window.LumiGame.WorldRenderer=WorldRenderer;
